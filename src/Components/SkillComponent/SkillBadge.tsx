import React, { FC } from 'react';

type TSkillBadge = {
  skillName: string;
  type: string;
  description?: string;
};

const SkillBadgeComponent: FC<TSkillBadge> = ({ skillName, type }) => {
  return <></>;
};

export default SkillBadgeComponent;
